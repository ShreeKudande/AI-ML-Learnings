#Q.Ask the user for a temerature in Celsius (string input). Convert it to float, then calculate and print temprature in Fahrenheit.

#Conversion formula : FahrenheitTemp = (CelsiusTemp*(9/5)) + 32

temp = input("Enter the temperatur in Celsius : ")
temp = float(temp)

print(temp*(9/5)+32)

# Enter the temperatur in Celsius : 32
# 89.6