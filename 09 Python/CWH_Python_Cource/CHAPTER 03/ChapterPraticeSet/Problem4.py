#Q.Replace the double space from problem 3 with single spaces.

user = input("Type Something: ")
print(user.replace("  ", " ")) #Strings are immutable which means that you cannot change them by running functions on them

#Type Something: sh  ree
# sh ree

